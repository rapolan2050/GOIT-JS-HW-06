/* eslint-disable no-undef */
/* eslint-disable no-constant-condition */

/* for (let monigote = 0; monigote >= 12; monigote = monigote + 2); {
    console.log(monigote);
} */

if (false != true) {
    console.log('That makes sense.');
    if (1 < 2) {
        console.log('No surprise there.');
    }
}